@using SaluScanner.Core.Entities
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<table class="table table-bordered">
    <tr>
        <th>ALERJİ ADI</th>
        <th>ALERJİ AÇIKLAMASI</th>
        <th>GÜNCELLE</th>
        <th>SİL</th>
    </tr>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>@item.AllergenName</th>
                <th>@item.AllergenDescription</th>
                <td><a class="btn btn-warning" asp-controller="Allergen" asp-action="Update" asp-route-id="@item.Id">GÜNCELLE</a></td>
                <td><a class="btn btn-danger" asp-controller="Allergen" asp-action="Remove" asp-route-id="@item.Id">SİL</a></td>
            </tr>
        }
    </tbody>
</table>

<a href="/Allergen/Add/" class="btn btn-primary">YENİ ALERJİ EKLE</a>


